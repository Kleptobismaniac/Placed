package klepto.placed.registry.item;

/*
import klepto.placed.Placed;
import net.fabricmc.fabric.api.itemgroup.v1.FabricItemGroup;
import net.fabricmc.fabric.api.itemgroup.v1.ItemGroupEvents;
import net.minecraft.block.Block;
import net.minecraft.item.Item;
import net.minecraft.item.ItemGroup;
import net.minecraft.item.ItemStack;
import net.minecraft.registry.RegistryKey;
import net.minecraft.util.Identifier;

public class ModItemGroup {
    Identifier id = Identifier.of(Placed.MOD_ID, "placed");

    public static final RegistryKey<ItemGroup> PLACED = FabricItemGroup.builder(Placed.ID .icon(() -> new ItemStack(ModItems.SOAP)).build();

    public static void addItem(Item item){
        ItemGroupEvents.modifyEntriesEvent(ModItemGroup.PLACED).register(content -> {content.add(item);});
    }

    public static void addBlock(Block block){
        ItemGroupEvents.modifyEntriesEvent(ModItemGroup.PLACED).register(content -> {content.add(block);});
    }
}
 */